This is a brief list of Umi API references. When you need to use a specific API and are unsure how to use it, you can use context7 MCP.

Note: When searching on context7, search for `Umi` (v4) instead of `UmiJS`, because `UmiJS` refers to the old v3 version.

# Umi Capabilities Index

This document provides an overview of Umi's capabilities based on its official documentation.

## Introduce

- **Contributing**: ❤️ Loving Umi and want to get involved? Thanks!
- **FAQ**: Yes, but it is not recommended to do so.
- **Design Philosophy**: Umi has gone from 1 to 4, experimenting with many things, both successes and failures. We have gradually accumulated these successful experiences, gui...
- **Upgrade to Umi 4**: Upgrading to Umi 4 can be completed in just a few easy steps, which can simply be described as - "Reinstall dependencies, modify configuration":

## Guides

- **Scaffolding**: Umi officially provides a scaffold, which allows you to easily and quickly create a project:
- **Route Data Loading**: Umi provides an out-of-the-box solution for data pre-loading, which can solve the waterfall requests of page components and data dependencies under mu...
- **Debugging**: In addition to using the browser's debugging tools for development debugging, Umi also recommends the following debugging methods to assist with proje...
- **Directory Structure**: This documents the directory structure conventionally (or recommended) in Umi projects. Please follow this directory structure to organize code during...
- **Environment Variables**: Umi can use environment variables for some special configuration and functions.
- **Micro Generators**: Umi comes with numerous micro generators to assist you in quickly completing some tedious work during development.
- **Getting Started**: First, you need to have node installed and make sure your node version is 18 or above. (It's recommended to use [nvm](https://github.com/nvm-sh/nvm) t...
- **Coding Standards**: We often use ESLint and Stylelint in projects to help us control coding quality. To achieve low-cost, high-performance, and more stable integration of...
- **MFSU**: MFSU is a package acceleration solution based on the new feature of webpack5, Module Federation. Its core idea is to divide and conquer by separating ...
- **Mock**: Umi provides an out-of-the-box Mock feature, which enables the setup of Mock data in a convenient and simple manner.
- **MPA Mode**: Umi supports the traditional MPA mode, under which the `src/pages` directory `*/index.[jt]sx?` files are taken as webpack entries for packing, without...
- **Plugin Development**: The essence of Umi lies in its plugin mechanism. Based on the plugin mechanism of Umi, you can enhance the build-time and runtime capabilities of your...
- **Development Environment**: This article will guide you through setting up a local development environment for an Umi.js project from scratch.
- **Proxy**: > A proxy, also known as a network proxy, is a special network service that allows one endpoint (usually the client) to connect indirectly to another ...
- **Routing**: In Umi applications, which are [single-page applications](https://en.wikipedia.org/wiki/Single-page_application), the navigation between pages is done...
- **Styles**: This document introduces various ways of using styles in Umi projects.
- **Testing**: Automated testing is an effective means to ensure quality, and Umi 4 provides scaffolding for unit testing. Umi 4 recommends using [Jest](https://jest...
- **TypeScript**: Umi enables TypeScript by default. If the project is created using the official scaffolding, the built-in files are mainly in the format of `xx.(ts|ts...
- **Plugins**: In a standard Umi application, by default, **no plugins are included**. If you want to use Max's features (such as data flow, antd, etc.), you will ne...
- **Using Vue**: This article introduces how to use Vue in Umi, where most of the configuration for Umi Vue is the same as React. Here we only list some configurations...

## Api

- **API**: For ease of search, the following content is sorted alphabetically.
  - **createBrowserHistory**: Creates a `BrowserHistory` that uses the browser's built-in `history` to track application navigatio...
  - **createHashHistory**: `createHashHistory` returns a `HashHistory` instance. The default `window` is the current document's...
  - **createMemoryHistory**: `MemoryHistory` is not operated or read from the address bar. It is also very suitable for testing a...
  - **createSearchParams**: A utility function that wraps `new URLSearchParams(init)`, supports creating with arrays and objects
  - **generatePath**: Generates the actual route to be accessed using the given path with parameters and the corresponding...
  - **Helmet**: The Helmet component provided by [react-helmet-async](https://github.com/staylor/react-helmet-async)...
  - **history**: Operations related to history, used to obtain current route information, execute route jumps, and li...
  - **Link**: `<Link>` is a React component, an `<a>` element with routing jump functionality.
  - **matchPath**: `matchPath` can match a given path with a known route format and return the match result.
  - **matchRoutes**: `matchRoutes` can match a given path with multiple potential route choices and return the match resu...
  - **NavLink**: `<NavLink>` is a special form of `<Link>`, aware of whether it is in an active routing state. Often ...
  - **Outlet**: `<Outlet>` is used to render child routes within a parent route. If the parent route is an exact mat...
  - **resolvePath**: Used to resolve front-end routing jump paths on the client side.
  - **terminal**: `terminal` is a tool for logging messages from the browser to the node terminal during the developme...
  - **useAppData**: `useAppData` returns global application data.
  - **useLocation**: `useLocation` returns the current location object.
  - **useMatch**: `useMatch` returns match information for the given path; if no match, it will return `null`
  - **useNavigate**: `useNavigate` hook function returns a function that can control jumping; for example, it can be used...
  - **useOutlet**: `useOutlet` returns the child route element currently matched, used internally by the `<Outlet>`.
  - **useOutletContext**: `useOutletContext` is used to return the `context` mounted on the `Outlet` component.
  - **useParams**: The `useParams` hook function returns a read-only key-value pair object of dynamic route matching pa...
  - **useResolvedPath**: `useResolvedPath` resolves the complete routing information for the target address based on the curr...
  - **useRouteData**: `useRouteData` returns the route data of the currently matched route.
  - **useRoutes**: `useRoutes` is a hook function to render routes, pass in route configuration and optional parameter ...
  - **useRouteProps**: Read the props attribute of the current route in the route configuration using this hook. You can us...
  - **useSelectedRoutes**: Used to read all the route information hit by the current path. For example, in a `layout`, it is po...
- **Command Line**: umi offers many built-in command line interfaces to start and build projects, as well as some auxiliary development commands, such as generators.
- **Configuration**: For the custom configurations available in umi, you can use the `.umirc.ts` file or `config/config.ts` in the project root directory. It's noteworthy ...
- **Plugin API**: The core of Umi lies in its plugin mechanism. With Umi's plugin mechanism, you can gain the ability to extend your project's compile-time and runtime ...
- **Runtime Configuration**: The difference between runtime configuration and configuration is that it runs on the browser side. Based on this, we can write functions, tsx, import...

## Max

- **Permissions**: Configuring activation. Requires `src/access.ts` to provide permission configuration.
- **Site Statistics**: `@umijs/max` has built-in site statistics functionality, currently supporting [Google Analytics](https://analytics.google.com/analytics/web/) and [Bai...
- **antd**: Integration of the antd component library.
- **Charts**: The Umi team recommends using [Ant Design Charts](https://charts.ant.design/) or [Pro Components](https://procomponents.ant.design/) in line with Ant ...
- **Data Stream**: `@umi/max` has a built-in **data flow management** [plugin](https://github.com/umijs/umi/blob/master/packages/plugins/src/model.ts), which is a lightw...
- **dva**: React components are essentially built through JSX and styles according to the state to form the final UI. It is the change in state that actually dyn...
- **Internationalization**: `@umi/max` comes with the [Internationalization plugin](https://github.com/umijs/umi/blob/master/packages/plugins/src/locale.ts), which easily integra...
- **Umi Max Introduction**: Umi, as an extensible enterprise-level front end application framework, has already served 10,000+ applications within Ant Group, either directly or i...
- **Layout and Menu**: Configure to enable.
- **Module Federation Plugin**: Using Module Federation functionality in Umi projects.
- **Micro Frontends**: `@umi/max` has a built-in **Qiankun Micro Frontends** [plugin](https://github.com/umijs/umi/blob/master/packages/plugins/src/qiankun.ts), which can en...
- **react-query**: @umijs/max comes with the [react-query](https://tanstack.com/query/) request strategy (which is the same as @tanstack/react-query).
- **Request**: `@umijs/max` has built-in plugin solution. It offers a unified network request and error handling solution based on [axios](https://axios-http.com/) a...
- **styled-components**: @umijs/max comes with the [styled-components](https://styled-components.com/) styling solution built-in.
- **valtio**: @umijs/max comes with a built-in valtio data flow solution.

