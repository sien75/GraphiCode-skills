# launcher.ts example

This is an example, you should replace it with the actual.

```ts
// import all states

import state1 from "<state1Dir>/state1";
import state2 from "<state2Dir>/state2";
import state3 from "<state3Dir>/state3";
import state4 from "<state4Dir>/state4";

// import all flows

import flow1 from "<flow1Dir>/flow1";
import flow2 from "<flow2Dir>/flow2";

// enable the necessary states

state1.enable();
state2.enable();
```
